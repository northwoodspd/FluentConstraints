# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

use_frameworks!

target 'FluentConstraints' do
end

target 'FluentConstraintsTests' do
    pod "Nimble", git: "https://github.com/Quick/Nimble", branch: "swift-2.0"
end
